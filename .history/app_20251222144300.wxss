/* app.wxss */
page {
  /* --- 🎨 调色板 --- */
  --cream-yellow: #FFF8E7; /* 奶油黄背景 */
  --peach-pink: #FFB7B2;   /* 蜜桃粉 */
  --peach-shadow: #E69A96; /* 粉色阴影 */
  --mint-green: #B5EAD7;   /* 薄荷绿 */
  --mint-shadow: #96CBAA;  /* 绿色阴影 */
  --sky-blue: #C7CEEA;     /* 天空蓝 */
  --night-blue: #3949AB;   /* 深夜蓝 */
  --text-main: #4A4A4A;    /* 主文字 */
  --text-light: #888888;   /* 浅色文字 */
  
  /* --- 📐 布局与字体 --- */
  font-family: "VAG Rounded", "PingFang SC", "Heiti SC", sans-serif; /* 尝试使用圆体 */
  background-color: var(--cream-yellow);
  color: var(--text-main);
  box-sizing: border-box;
}

/* 通用 2.5D 卡片风格 */
.card-box {
  background: #FFFFFF;
  border-radius: 40rpx;
  border: 4rpx solid #333;
  box-shadow: 10rpx 10rpx 0rpx rgba(0,0,0,0.15); /* 硬阴影，增加拟物感 */
  overflow: hidden;
  transition: all 0.1s ease;
}

/* 按钮点击态：按下效果 */
.hover-btn {
  transform: translate(4rpx, 4rpx) !important;
  box-shadow: 2rpx 2rpx 0rpx rgba(0,0,0,0.15) !important;
}

/* 通用弹性居中 */
.flex-center {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* 隐藏滚动条 */
::-webkit-scrollbar {
  width: 0;
  height: 0;
  color: transparent;
}