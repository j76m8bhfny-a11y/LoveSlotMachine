<view class="result-page">
  
  <!-- 场景1：复古老虎机 (Loading) -->
  <view class="loading-wrapper" wx:if="{{isLoading}}">
    
    <view class="slot-machine-wrapper">
      <view class="machine-roof">
        <view class="lights">
          <view class="light" wx:for="{{[1,2,3,4]}}" wx:key="*this"></view>
        </view>
      </view>
      
      <view class="machine-body">
        <view class="screen-frame">
          <!-- 三列滚动窗口 -->
          <view class="reel-container">
            <view class="reel" style="animation-duration: 0.8s">
              <view class="reel-inner {{spinning ? 'blur-spin' : ''}}">
                <text>🎡</text><text>🎪</text><text>🎨</text><text>🎡</text>
              </view>
            </view>
            <view class="reel" style="animation-duration: 1.2s">
               <view class="reel-inner {{spinning ? 'blur-spin' : ''}}">
                <text>🍔</text><text>🥗</text><text>🍱</text><text>🍔</text>
              </view>
            </view>
            <view class="reel" style="animation-duration: 1.0s">
               <view class="reel-inner {{spinning ? 'blur-spin' : ''}}">
                <text>❤️</text><text>✨</text><text>🎁</text><text>❤️</text>
              </view>
            </view>
            <!-- 反光遮罩 -->
            <view class="glass-glare"></view>
          </view>
        </view>
        
        <view class="machine-label">AI LOVESLOT</view>
      </view>
    </view>

    <!-- ✨✨ 新增：多巴胺分析气泡 ✨✨ -->
    <view class="analysis-card">
      <view class="card-header">
        <text class="emoji-icon">🤖</text>
        <text class="header-title">灵感分析中...</text>
      </view>
      <scroll-view scroll-y class="card-body" scroll-top="{{scrollTop}}" scroll-with-animation>
        <block wx:for="{{analysisLogs}}" wx:key="index">
          <view class="log-bubble">
            <!-- 根据日志类型显示不同的前缀图标 -->
            <text wx:if="{{item.type==='relation'}}">💞</text>
            <text wx:elif="{{item.type==='weather'}}">🌦️</text>
            <text wx:elif="{{item.type==='budget'}}">💰</text>
            <text wx:else>✨</text>
            <text>{{item.text}}</text>
          </view>
        </block>
      </scroll-view>
    </view>

  </view>

  <!-- 场景2：出票结果 (Result) -->
  <view class="receipt-scene {{showReceipt ? 'enter' : ''}}" wx:else>
    
    <view class="receipt-paper">
      <!-- CSS 锯齿 -->
      <view class="sawtooth-top"></view>
      
      <view class="receipt-body">
        <view class="r-header">
          <text class="r-title">心动小票</text>
          <text class="r-date">{{resultData.date}}</text>
        </view>
        <view class="r-divider">--------------------------------</view>
        
        <view class="r-main">
          <text class="plan-name">{{result.title}}</text>
          <view class="stars">❤❤❤❤❤</view>
        </view>
        
        <view class="r-kv">
          <view class="kv-row">
            <text class="k">📍 地点</text>
            <text class="v">{{result.location}}</text>
          </view>
          <view class="kv-row activity-row">
            <text class="k">🎡 玩法</text>
            <text class="v highlight">{{result.activity}}</text>
          </view>
        </view>
        
        <view class="r-box">
          <text class="box-label">💡 推荐理由</text>
          <text class="box-content">{{result.reason}}</text>
        </view>
        
        <view class="r-tags">
          <block wx:for="{{result.tags}}" wx:key="*this">
            <text class="r-tag">#{{item}}</text>
          </block>
        </view>
        
        <view class="r-divider">--------------------------------</view>
        <view class="r-footer">
          <text class="barcode">|| | ||| || || | |||</text>
          <text>Keep Loving</text>
        </view>
      </view>
      
      <view class="sawtooth-bottom"></view>
    </view>

    <!-- 交互手柄区域 -->
    <view class="action-dock">
      <button class="handle-btn reroll" bindtap="reRoll" hover-class="hover-btn">
        <text>🕹️ 不满意？拉杆重试</text>
      </button>
      <button class="share-btn" open-type="share" hover-class="hover-btn">
        <text>📤 分享给Ta</text>
      </button>
    </view>
  </view>
</view>