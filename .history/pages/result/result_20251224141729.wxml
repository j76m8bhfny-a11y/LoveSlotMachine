<view class="result-page">
  
  <view class="loading-wrapper" wx:if="{{isLoading}}">
    
    <view class="slot-machine-wrapper">
      
      <view class="machine-body-image-wrapper {{isFlowing ? 'flowing' : ''}}"> 
        
        <image class="machine-img base-img" src="/assets/images/lever_0.png" mode="scaleToFill" ></image>
        <image class="machine-img overlay-img" src="/assets/images/lever_1.png" mode="scaleToFill" 
               style="opacity: {{leverFrame === 1 ? 1 : 0}}"></image>     
        <image class="machine-img overlay-img" src="/assets/images/lever_2.png" mode="scaleToFill" 
               style="opacity: {{leverFrame === 2 ? 1 : 0}}"></image>     
        <image class="machine-img overlay-img" src="/assets/images/lever_3.png" mode="scaleToFill" 
               style="opacity: {{leverFrame === 3 ? 1 : 0}}"></image>

        <view class="screen-frame-overlay" style="z-index: 999;">
           <view class="reel-container">
            <view class="reel">
              <view class="reel-inner {{spinning ? 'spinning' : 'stopped'}}" style="animation-duration: 0.6s">
                <block wx:for="{{reel1}}" wx:key="index">
                  <view class="reel-item">{{item}}</view>
                </block>
              </view>
            </view>
            <view class="reel">
              <view class="reel-inner {{spinning ? 'spinning' : 'stopped'}}" style="animation-duration: 0.9s">
                <block wx:for="{{reel2}}" wx:key="index">
                  <view class="reel-item">{{item}}</view>
                </block>
              </view>
            </view>
            <view class="reel">
              <view class="reel-inner {{spinning ? 'spinning' : 'stopped'}}" style="animation-duration: 0.75s">
                <block wx:for="{{reel3}}" wx:key="index">
                  <view class="reel-item">{{item}}</view>
                </block>
              </view>
            </view>
            
            <view class="glass-glare"></view>
            <view class="win-line"></view>
          </view>
        </view>

        
      </view>
    </view>

    <view class="analysis-card">
      <view class="card-header">
        <text class="emoji-icon">🤖</text>
        <text class="header-title">灵感分析中...</text>
      </view>
      <scroll-view scroll-y class="card-body" scroll-top="{{scrollTop}}" scroll-with-animation>
        <block wx:for="{{analysisLogs}}" wx:key="index">
          <view class="log-bubble">
            <text wx:if="{{item.type==='relation'}}">💞</text>
            <text wx:elif="{{item.type==='weather'}}">🌦️</text>
            <text wx:elif="{{item.type==='budget'}}">💰</text>
            <text wx:else>✨</text>
            <text>{{item.text}}</text>
          </view>
        </block>
      </scroll-view>
    </view>

  </view>

  <view class="receipt-scene {{showReceipt ? 'enter' : ''}}" wx:else>
     <view class="receipt-paper">
      <view class="sawtooth-top"></view>
      <view class="receipt-body">
        <view class="r-header">
          <text class="r-title">心动小票</text>
          <text class="r-date">{{result.date || 'Today'}}</text>
        </view>
        <view class="r-divider">--------------------------------</view>
        <view class="r-main">
          <text class="plan-name">{{result.title}}</text>
          <view class="stars">❤❤❤❤❤</view>
        </view>
        <view class="r-kv">
          <view class="kv-row"><text class="k">📍 地点</text><text class="v">{{result.location}}</text></view>
          <view class="kv-row activity-row"><text class="k">🎡 玩法</text><text class="v highlight">{{result.activity}}</text></view>
        </view>
        <view class="r-box">
          <text class="box-label">💡 推荐理由</text>
          <text class="box-content">{{result.reason}}</text>
        </view>
        <view class="r-tags">
          <block wx:for="{{result.tags}}" wx:key="*this"><text class="r-tag">#{{item}}</text></block>
        </view>
        <view class="r-divider">--------------------------------</view>
        <view class="r-footer">
          <text class="barcode">|| | ||| || || | |||</text>
          <text>Keep Loving</text>
        </view>
      </view>
      <view class="sawtooth-bottom"></view>
    </view>

    <view class="action-dock">
      <button class="handle-btn reroll" bindtap="reRoll" hover-class="hover-btn">
        <text>🕹️ 不满意？再拉一次</text>
      </button>
      <button class="handle-btn share" open-type="share" hover-class="hover-btn">
        <text>📤 分享给Ta</text>
      </button>
    </view>
  </view>
</view>